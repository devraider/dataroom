export enum Theme {
  LIGHT = "light",
  DARK = "dark",
  SYSTEM = "system",
}

export enum StorageKey {
  AUTH = "auth-storage",
  THEME = "theme-storage",
}

export enum ImportProgress {
  PENDING = "pending",
  UPLOADING = "uploading",
  SUCCESS = "success",
  ERROR = "error",
}
